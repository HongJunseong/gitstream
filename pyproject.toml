[tool.pytest.ini_options]
pythonpath = ["."]
testpaths  = ["tests"]

[tool.ruff]
line-length = 100
exclude     = ["databricks/notebooks/"]   # dbutils 는 Databricks 전용

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]   # line too long — 가독성 우선
